{
  "name": "manyvm",
  "version": "1.0.0",
  "description": "This GitHub Action uses QEMU to run VMs like FreeBSD and in different CPU architectures like aarch64, riscv64 and etc.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "expand-home-dir": "^0.0.3"
  }
}
